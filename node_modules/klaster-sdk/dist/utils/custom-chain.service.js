import { defineChain } from "viem";
export function defineCustomChain(chainId, rpcUrl) {
    return defineChain({
        id: chainId,
        network: "chain-".concat(chainId),
        name: "Chain ".concat(chainId),
        nativeCurrency: {
            decimals: 18,
            name: "_",
            symbol: "_",
        },
        rpcUrls: {
            default: { http: [rpcUrl] },
            public: { http: [rpcUrl] },
        },
    });
}
//# sourceMappingURL=custom-chain.service.js.map
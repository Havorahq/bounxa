import { encodeFunctionData, parseAbi, stringToHex } from "viem";
export function encodeSmartAccountCall(chainId, txs) {
    var abi = parseAbi([
        "function execute(address[] calldata to, uint256[] calldata value, bytes[] calldata data)",
    ]);
    var data = encodeFunctionData({
        abi: abi,
        functionName: "execute",
        args: [
            txs.map(function (tx) { return tx.to; }),
            txs.map(function (tx) { var _a; return (_a = tx.value) !== null && _a !== void 0 ? _a : BigInt(0); }),
            txs.map(function (tx) { var _a; return (_a = tx.data) !== null && _a !== void 0 ? _a : stringToHex(''); }),
        ],
    });
    return {
        callData: data,
        callGasLimit: txs
            .map(function (x) { return x.gasLimit; })
            .reduce(function (curr, acc) { return curr + acc; })
            .toString(),
        chainId: chainId,
    };
}
//# sourceMappingURL=encoding.service.js.map
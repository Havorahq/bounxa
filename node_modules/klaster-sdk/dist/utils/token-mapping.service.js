export function getTokenAddressForChainId(tokenMapping, chainId) {
    var _a;
    return (_a = tokenMapping.find(function (deployment) { return deployment.chainId === chainId; })) === null || _a === void 0 ? void 0 : _a.address;
}
export function buildTokenMapping(tokens) {
    var mapping = [];
    return tokens.map(function (token) {
        return {
            address: token.address,
            chainId: token.chainId
        };
    });
}
export function deployment(chainId, address) {
    return {
        chainId: chainId,
        address: address
    };
}
//# sourceMappingURL=token-mapping.service.js.map
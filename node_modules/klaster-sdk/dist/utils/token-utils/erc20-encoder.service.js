import { encodeFunctionData, erc20Abi } from "viem";
import { rawTx } from "../itx.service";
export function encodeApproveTx(_a) {
    var tokenAddress = _a.tokenAddress, recipient = _a.recipient, amount = _a.amount;
    return rawTx({
        gasLimit: BigInt(100000),
        to: tokenAddress,
        data: encodeFunctionData({
            abi: erc20Abi,
            functionName: 'approve',
            args: [recipient, amount]
        }),
        value: BigInt(0)
    });
}
//# sourceMappingURL=erc20-encoder.service.js.map